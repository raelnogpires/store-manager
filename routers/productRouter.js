// const express = require('express');
// const rescue = require('express-rescue');
// const productsController = require('../controllers/productsController');
// // const productValidation = require('../middlewares/productValidation');

// const router = express.Router();

// router.get('/', rescue(productsController.getAll));
// router.get('/:id', rescue(productsController.getById));
// // router.post('/', productValidation, rescue(productsController.create));
// // router.put('/:id', productValidation, rescue(productsController.update));
// // router.delete('/:id', productsController.deleteById);

// module.exports = router;